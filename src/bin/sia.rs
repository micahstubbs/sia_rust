//! `sia` binary entry point. Full CLI/orchestrator wiring lands with issues #7/#9.

fn main() {
    eprintln!("sia: CLI entry point not yet wired (Rust port in progress).");
    std::process::exit(1);
}
