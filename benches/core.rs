//! Criterion benchmarks for the deterministic SIA core.
//!
//! Stub scaffolding — populated in issue #31. `harness = false` (see Cargo.toml)
//! means this file owns `main`; Criterion generates it via `criterion_main!`.

fn main() {
    eprintln!("benches/core.rs: not yet implemented (issue #31)");
}
